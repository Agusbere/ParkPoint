        @{
            ViewData["Title"] = "Index";
            var listaCoordenadas = ViewBag.ListaCoordenadas as List<Estacionamiento>;
        }

        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        
        <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
            integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
            crossorigin=""/>

            <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
            integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
            crossorigin=""></script>


        <!-- @{
        ViewBag.MyData = "Ziggy, Rafiq Blog Post";
        }
            <input type="hidden" id="my-data" value="@ViewBag.MyData" />
            <script>
                var myData = document.getElementById("my-data").value;
                console.log(myData); // Output: "Ziggy, Rafiq Blog Post"
            </script> -->

            <div class="todo-mapa">
                <div id="map"></div>
                <div class="barra-busqueda">
                    <img src="~/img/lupa.png" alt="busqueda">
                    <input type="text" placeholder="Buscar destino...">
                </div>
                <div class="tiempo-zona">
                    Tiempo estimado en la zona: 15 minutos
                </div>
                <div class="conjunto-botones-de-accion">
                    <a class="reportar" href="@Url.Action("Reportar")">
                        <img src="~/img/advertencia.png" alt="reporte">
                        Reportar
                    
                </div>
            </div>
            

            
<div class="modal-card">
  <div class="modal-card-content">
    <p class="modal-card-heading">OCUPAR ESPACIO</p>
    <p class="modal-card-description">Desea ocupar el espacio en la ubicación seleccionada (calle + altura)</p>
  </div>
  <div class="modal-card-button-wrapper">
    <button class="modal-card-button secondary">Cancelar</button>
    <button class="modal-card-button primary" onclick="enviarDatosAControlador('${calle}', '${altura}', '${selectedLat}', '${selectedLon}')" >Confirmar</button>
  </div>
  <button class="modal-exit-button">

  </button>
</div>

            <script>
                @ViewBag.Ubicaciones
        </script>
            
<!--necesito que cuando en el js toques ocupar espacio en cada pin de estacionamiento del mapa, se abra el modal cuyo dise°no ya esta hecho , que esta en el index lo podres ver, y se le manda la calle y altura de ese pin ppara que en el mensajito diga: queres ocupar en la clale  y altura seleccionada? y ahi aparece esa calle y altura. eso mediante otro ajax. dsp de eso e envian los datos al controlador de esa calle y altura con la ubicacion en x ey en y l logica ya esta en enviardatosacontrolador. eso si toca aceptar en el modal. si toca caneclar se cierra eñ pin y el modal.
>

            
        

